version: '3.8'

services:
  isle-server:
    build:
      context: ./src
      dockerfile: server/Dockerfile
    environment:
      - PORT=${SERVER_PORT:-8080}
      - PYTHONUNBUFFERED=1
    ports:
      - "${SERVER_PORT:-8080}:8080"
    restart: unless-stopped
